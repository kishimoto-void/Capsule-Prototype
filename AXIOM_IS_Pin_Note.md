# min3 IS ピン（作業線の狭い改良）

日付: 2026-09-04  
対象: `axiom_min3.py` のみ。金型 `axiom_min.py` は未変更。  
生成文は見ていない。層は足していない。

## 何を変えたか

旧 IS は行ログだった。同じ住所に閉じた語を4つ採用すると、最初の行が落ちた。弾幕の「状態=試作2を回している」が消えたのはこの経路である。

新 IS は次の閉じた政策だけを足す。

1. 同じ語は上書き。`状態=先` のあと `状態=後` は1行のまま。
2. 4語目が来たら、ピン以外の最古を落とす。ピンは `状態`。
3. ピンしか無いときに溢れたら最古（ピン含む）。
4. `ingest` の `evicted` と `last_is_evicted` は監査。記憶層ではない。
5. `restore` も同じ折りたたみを通す。

IS_MAX=3 は据え置き。Render は IS だけ。Δ を見える世界に混ぜない。

## 測ったこと

`python3 -m unittest test_axiom_min3.py` → 51/51 OK（旧47 + 新規4）。

Phase 3 を同じ山で再実行:

| 条件 | 文字 |
|------|------|
| B | 3351 |
| M2 | 1355 |
| M3 | 1562 |

M3 の p1/p4/p5 で `試作2` が visible。M2 は従来どおり `試作2` が欠ける（min2 は未変更）。

隠れ住所の leaked は M3 で空のまま。

## トレードオフ

ピンは `状態` を残すために、同じ住所の最古の非ピン（多くの場合 `課題`）を落とす。
「今どの辺だ？」は見える。「何が未測か？」は IS から落ちることがある。
課題を残したいなら、4語目を採用しないか、ピン集合を変える。ピンを増やすと IS_MAX と衝突する。

## 言わないこと

- ハルシネーション一般が減った
- 生成ゲートになった
- min3 が min2 よりモデル応答で強い
